pub mod packet;

fn main() {
    /* Game plan:
     * - Define data structure and: Element = Int or [Element]
     * - Parse input into data structure, record indices for solution /!\ 1-based indexing /!\
     * - Implement and test comparison rules
     * - Sum correct indices
     */
    println!("Hello, world!");
}
